# Test Environment Configuration
NODE_ENV=test

# API Configuration
API_PORT=3002

# Test Database (IMPORTANT: Create a separate test database or use the same database with caution)
# Option 1: Use the same database as development (tests will clean data between runs)
# Option 2: Create a separate test database (recommended for CI/CD)
DATABASE_URL="postgresql://neondb_owner:npg_7mbK5CwRyniO@ep-dawn-rain-ab59rnm5-pooler.eu-west-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require"

# Test Redis (use same instance as development - tests will use key prefixes for isolation)
# Note: Some Redis instances don't support multiple databases, so we use the same database with prefixed keys
REDIS_URL="redis://default:bmO0YKQltIKNyCe3DqrKH0RWpgdxnGFy@redis-12622.c243.eu-west-1-3.ec2.cloud.redislabs.com:12622"

# JWT Authentication (test-specific secret)
JWT_SECRET="test-secret-key-for-integration-tests-only-not-for-production"
JWT_EXPIRES_IN="24h"

# Frontend URL (for CORS)
FRONTEND_URL="http://localhost:3000"
